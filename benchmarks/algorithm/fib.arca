// Arca Fibonacci Benchmark
// Clean high-level application code using std/time

import { Instant } from "std/time"

fn fib(n: i32) -> i32 {
  if n <= 1 { n }
  else { fib(n - 1) + fib(n - 2) }
}

fn main() {
  let timer = Instant.now()
  let result = fib(45)
  println("Arca fib(45) = ", result, " (", timer.elapsed_ms(), "ms)")
}
