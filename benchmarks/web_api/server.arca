// Arca Web API Server Benchmark
// Declarative Bun/Express style Arca HTTP Server API (std/http)

import { serve } from "std/http"

fn main() {
  serve({
    port: 3000,

    get("/") {
      {
        message: "hello"
      }
    }
  })
}
