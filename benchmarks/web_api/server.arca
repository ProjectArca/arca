// Arca Web API Server Benchmark
// Bun-style HTTP server (std/http)
// Parses as: serve({ port: 3000, fetch(req) { Response.json(...) } })

import { serve, Response } from "std/http"

fn main() {
  serve({
    port: 3000,

    fetch(req) {
      Response.json({ message: "hello" })
    }
  })
}
