// Arca Fibonacci
// Recursive fibonacci with timing

import { Instant } from "std/time"

fn fib(n: i32) -> i32 {
  if n <= 1 { n }
  else { fib(n - 1) + fib(n - 2) }
}

fn main() {
  let t = Instant.now()
  println("fib(45) =", fib(45), "(", t.elapsed_ms(), "ms)")
}
