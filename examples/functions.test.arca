// Test: functions and recursion
fn fib(n: i32) -> i32 {
  if n <= 1 { n }
  else { fib(n - 1) + fib(n - 2) }
}

fn add(a: i32, b: i32) -> i32 {
  a + b
}

fn main() {
  println(add(10, 20))
  println(fib(10))
}
