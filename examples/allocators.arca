// Example: Explicit Memory Allocators in Arca (std/alloc)

import { Arena, Pool } from "std/alloc"

fn main() {
  let arena = Arena.new()
  let pool = Pool.new(64)

  println("Explicit arena & pool allocators initialized successfully.")
}
