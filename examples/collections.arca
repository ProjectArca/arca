// Example: Standard Collections in Arca (std/collections)

import { Array, Map, Set } from "std/collections"

fn main() {
  let users = Array.new()
  let cache = Map.new()
  let tags = Set.new()

  println("Array, Map, and Set initialized cleanly.")
}
