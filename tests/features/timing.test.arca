// Test: timing
// Expected output:
// 55
// [non-negative integer]
import { Instant } from "std/time"

fn fib(n: i32) -> i32 {
  if n <= 1 { n }
  else { fib(n - 1) + fib(n - 2) }
}

fn main() {
  let f = fib(10)
  println(f)
  let t = Instant.now()
  let e = t.elapsed_ms()
  println(e)
}
