// Arca Standard Allocators (std/alloc)

struct Arena {
  capacity: i64
}

extend Arena {
  fn new() -> Arena {
    Arena { capacity: 4096 }
  }
}

struct Pool {
  block_size: i64
}

extend Pool {
  fn new(block_size: i64) -> Pool {
    Pool { block_size: block_size }
  }
}
